<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_fingerprint_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.FingerprintActivity">

    <include layout="@layout/default_titlbar_trans" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/titleBar"
        android:layout_centerHorizontal="true"
        android:src="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/tvVerifySelect"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"
        android:layout_marginBottom="60dp"
        android:text="切换解锁方式"
        android:textColor="@color/blue_color"
        android:textStyle="bold"
        android:visibility="invisible" />

    <ImageView
        android:id="@+id/ivVerifyStart"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/tvVerifyStart"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="25dp"
        android:background="@drawable/fingerprint_normal" />

    <TextView
        android:id="@+id/tvVerifyStart"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/tvVerifySelect"
        android:layout_centerHorizontal="true"
        android:textColor="@color/white_color_assist_2"
        tools:text="进行身份验证解锁登录" />
</RelativeLayout>
